---
import Button from '@components/ui/button.astro';
---

<section class="relative py-20 overflow-hidden wave-bg">
    <div class="container flex flex-col md:flex-row items-center">
        <div class="flex-1 space-y-6 text-center md:text-left">
        <h1 class="text-4xl md:text-6xl font-extrabold leading-tight">
            Learn Spanish, <span class="text-warm-orange">For Real.</span>
        </h1>
        <p class="text-xl md:text-2xl text-foreground/80">
            Interactive, real-life lessons with native speakers.
        </p>
        <div class="pt-4">
            <Button 
                variant="spanish"
                size="lg" 
                class="rounded-full text-lg shadow-lg"
            >
                Prueba una Lección Gratis
            </Button>
        </div>
        </div>
        <div class="flex-1 mt-12 md:mt-0 flex justify-center animate-float">
        <div class="relative w-72 h-72 md:w-96 md:h-96">
        </div>
        </div>
    </div>

    {/* Decorative elements */}
    <div class="absolute -bottom-6 left-0 w-full h-12 bg-background" style={{ clipPath: 'polygon(0 100%, 100% 100%, 100% 0, 0 100%)' }} />
</section>